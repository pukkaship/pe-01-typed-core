# Module 1 needs no secrets — it runs fully locally with no network, no AI, no database.
# This file exists only so you build the habit of keeping secrets out of source.
#
# A later module will need a key like the line below. Never commit the real value.
# LLM_API_KEY=
